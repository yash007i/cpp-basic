#include <iostream>

using namespace std;

int main() {

    int bitOR;
    int bitAND;
    int num1, num2;

    bitOR = 4 | 5;
    bitAND = 2 & 3;
    cout << "Bitwise OR is: " << bitOR << endl;
    cout << "Bitwise AND is: " << bitAND << endl;
    // Compilement, left shift, right shift, XOR

    return 0;
}